import { HoverHighlightDirective } from './hover-highlight.directive';

describe('HoverHighlighDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
